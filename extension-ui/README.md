# Flow Bridge side panel

Chrome side panel UI (shadcn + React). Built files land in `../extension/ui` so `Load unpacked` still points at `extension/`.

```bash
npm install
npm run build
```

Then reload the unpacked extension. Toolbar icon opens this panel — there is no popup.
